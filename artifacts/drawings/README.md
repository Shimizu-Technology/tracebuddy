# Drawing contact sheets

These generated SVGs show the 54 active picker drawings at the two review sizes. Eighteen retired IDs remain available only for saved-work compatibility:

- `contact-sheet-160.svg` checks gallery-thumbnail recognition.
- `contact-sheet-420.svg` checks the native tracing canvas.

Regenerate both files with `npm run drawings:contact-sheets`. Validate the catalog first with `npm run drawings:validate`.
